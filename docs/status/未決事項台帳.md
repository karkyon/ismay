# 未決事項台帳

DOC-13 §7（DEC-001〜006）への追補。**推測で実装しない**項目と、未着手の項目を記録する。

| 項目 | 値 |
|---|---|
| 更新 | 2026-09-26（DOC-SYNC-05） |
| 基準HEAD | `d3844e475159e733b048a5f8c16bc1da81f30f7b`（SECURITY-ENCRYPTION-01A適用後） |
| 全体台帳 | [ISMAY_全機能トレーサビリティ_実装状況台帳](./ISMAY_全機能トレーサビリティ_実装状況台帳.md) |

## 1. Purge関連
| ID | 内容 | 状態 | 停止・保留している範囲 | 決定に必要なこと |
|---|---|---|---|---|
| OPEN-PURGE-01 | Platform Admin（全テナント横断の管理者）契約 | 決定待ち | HTTP Purge（dry-run/execute）はfail closed | ロールの定義、付与手順、Workspace RBACとの分離、監査要件 |
| OPEN-PURGE-02 | `consents`の法定保持 | 暫定決定（物理削除） | — | 法定保持の要否と期間。要件があればDEC-PURGE-02B §4.4を案Cへ改訂 |
| OPEN-PURGE-03 | `audit_logs`の保持期間と、`target_id`（UUID）を仮名のまま残すことの可否 | 暫定決定（無期限保持＋ip_addressの墨消し） | 保持期間Job | 保持期間（DOC-09 §4 policy table） |
| OPEN-PURGE-04 | 個別エンティティのsoft delete（Responsibility単体の`deleted_at`等）の30日Purge | 未着手 | 単体削除した行は物理削除されない | 対象表、削除伝播（DOC-09 §5）の範囲 |
| OPEN-PURGE-05 | scope列がNULLの旧行（migration時に集約を解決できなかった孤立行） | 件数の表示のみ | 自動削除しない | 旧行を一括削除してよいかの判断（運用者が明示的に実行する別ツール） |
| OPEN-PURGE-06 | backup・search index・cacheの削除期限（DOC-09 §5） | 未着手 | backupからの復元時にPurge済みデータが戻り得る | RPO/RTO/backup retention（DOC-13 DEC-006と同時に決定） |
| OPEN-PURGE-07 | Purgeの定期自動実行（スケジューラ） | 未着手（手動CLIのみ） | — | 無人で実行してよいか、失敗時の通知先 |

## 2. 認証（AUTH-EMAIL-01）関連
| ID | 内容 | 状態 | 停止・保留している範囲 | 決定に必要なこと |
|---|---|---|---|---|
| OPEN-AUTH-01 | 本番のSMTP事業者・送信ドメイン（SPF/DKIM/DMARC）・`APP_BASE_URL` | 決定待ち | 既定は`MAIL_TRANSPORT=log`（実送信しない） | 事業者と送信ドメイン、`app/.env`への設定（[MAIL_RUNBOOK](../runbooks/MAIL_RUNBOOK.md)） |
| OPEN-AUTH-02 | 永続的なIP/端末単位のrate limit（SEC-RATE） | 未着手 | メール発行はDBの発行履歴で制限済み。ログイン失敗のロックはプロセス内メモリのまま。`X-Forwarded-For`は信頼するproxyを限定していない | proxy構成、Redis等の採否 |
| OPEN-AUTH-03 | メールアドレス変更機能 | 未着手 | 変更APIが無い（token消費時の`sent_to_email`照合のみ先に実装） | 変更時の再確認フロー、旧アドレスへの通知 |
| OPEN-AUTH-04 | 未確認のまま放置されたアカウントの扱い | 未着手 | 未確認アカウントは残り続ける（ログインは不可） | 自動削除の要否と期間 |
| OPEN-AUTH-05 | 再設定リンクの期限（60分）・IP上限（1時間20件） | 実装上の既定値 | — | 値の承認（`emailTokenCore.ts`の定数で変更可） |
| OPEN-AUTH-MEMBER-01 | メンバー招待・Team共有（AUTH-MEMBER-01） | **BLOCKED_DECISION** | 招待・承諾・退出は実装しない。招待tokenだけ・feature flagで隠したschemaだけ、といった先行実装も行わない（契約なしの土台が将来の制約になるため） | 理由：現行の業務APIはWorkspace単位のscopeが中心で（`ensureDefaultWorkspace`は最古の有効membershipを1件選ぶ）、Entity Visibilityの横断強制が無い。招待を有効にすると、同じWorkspaceの他memberからCapture原文・Responsibility・Formation・Project Context等の個人データが見え得る。正本DOC-09 §6・DOC-12 §9のTEAM-1停止条件（管理者権限・Visibility UXの承認前は着手不可）にも該当する。決定事項：personal/team workspaceの分離、active workspaceの選択、Role×Actionのmatrix、PRIVATE/CONTEXT/WORKSPACE/EXPLICITの適用範囲、退出・owner移管・最後のowner、user削除時の共有データの所有権、Team privacy（k閾値・差分privacy） |

## 3. セキュリティ
| ID | 内容 | 状態 | 停止・保留している範囲 | 決定に必要なこと |
|---|---|---|---|---|
| OPEN-SECURITY-ENCRYPTION-01 | 機微データ暗号化の契約（TBD-17） | **DECISION_REQUIRED** | 暗号化対象の拡張・schema変更・backfill・鍵形式の変更は行わない（現状：TOTP秘密鍵とAI credentialのみAES-256-GCM、key versionなし） | [DEC-SECURITY-ENCRYPTION-01](../decisions/DEC-SECURITY-ENCRYPTION-01.md) §7の未決質問（方式、対象範囲、backup・鍵の保管、復号失敗時の挙動、rotation） |

## 4. 既存（DOC-13 §7から継続）
| ID | 内容 | 状態 |
|---|---|---|
| DEC-001 | Team aggregateのk閾値・差分privacy | 未決 |
| DEC-002 | AI providerの地域・保持・training policy | 未決 |
| DEC-003 | Activity Evidenceの収集粒度・保持 | 未決 |
| DEC-004 | Meridianへのwrite-backの責任分界・競合UI | 未決 |
| DEC-005 | 音声原本の保持期間・法域 | 未決（アカウントPurgeでは音声objectを削除済み） |
| DEC-006 | RPO/RTO/backup retention | 未決 |

## 5. 解決済み
| ID | 内容 | 決定 |
|---|---|---|
| DEC-PURGE-02B | FKを持たない表の扱い、Object Storageの削除順序 | ACCEPTED 2026-09-25（明示的scope列で削除。6段の順序で固定） |
| AUTH-EMAIL-01 | メール送信方式・未確認ユーザーの扱い・期限・再送制限 | 2026-09-26利用者決定（SMTP+nodemailer、未確認はログイン不可、確認リンク24時間・1回限り、再送60秒間隔かつ1時間5回、新リンクで旧リンク無効、パスワード再設定も同Gate）。[ADD-2026-09-26-AUTH-EMAIL](../spec-addenda/ADD-2026-09-26-AUTH-EMAIL.md) |
