import { AppShell } from "@/components/app/AppShell";
import { PemOnboardingClient } from "@/components/pem/PemOnboardingClient";

export default function PemOnboardingPage() {
  return (
    <AppShell>
      <PemOnboardingClient />
    </AppShell>
  );
}
