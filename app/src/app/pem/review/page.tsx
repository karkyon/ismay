import { AppShell } from "@/components/app/AppShell";
import { WeeklyReviewClient } from "@/components/pem/WeeklyReviewClient";

export default function WeeklyReviewPage() {
  return (
    <AppShell>
      <WeeklyReviewClient />
    </AppShell>
  );
}
